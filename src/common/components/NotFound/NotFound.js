import styles from "./notfound.module.css";

const NotFound = () => (
  <aside className={styles["search-error"]}>
    <h2>
      <span>Not found</span>
    </h2>
  </aside>
);

export default NotFound;
